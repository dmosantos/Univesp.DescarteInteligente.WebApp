import { createStore } from 'vuex'
import modules from './modules'

const state = {
}

const mutations = {
}

const actions = {
}

export default createStore({
    state,
    mutations,
    actions,
    modules
})